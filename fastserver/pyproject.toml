[project]
name = "fastserver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.14",
    "httpx>=0.28.1",
    "jinja2>=3.1.6",
    "loguru>=0.7.3",
    "uvicorn>=0.35.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]

[tool.uv]
default-groups = []
